
const divide = (x, y) => {
    console.log (`Dividing ${x}`);
    console.log (`Dividing ${y}`);
    return x / y;
}

divide(200,100)


let x = 2
const square = x => x * x 

const add = (a, b) => {
    console.log (`Adding ${a}`);
    console.log (`Adding ${b}`)
    return a + b;

}
add(3, 4)